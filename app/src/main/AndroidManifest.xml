<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".presentation.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.TestActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.activity.QuestionsActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.BreakTimeActivity"
            android:exported="true"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".presentation.activity.LearnMoreFavActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.HelpActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.ResultActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.MatchActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.FavoritesActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".presentation.activity.ReviseActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

    </application>

</manifest>